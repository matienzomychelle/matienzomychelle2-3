import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'App-dev-act2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
